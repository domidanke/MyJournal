void main() {
  // TODO: Needs a complete test along the lines of data coming from my_journal_screen entry coming from firebase get request
//  testWidgets('View Entry Screen is loaded and rendered properly',
//      (WidgetTester tester) async {
//    await tester.pumpWidget(MaterialApp(home: ViewEntry()));
//    expect(find.byType(ViewEntry), findsOneWidget);
//  });
}
